github_checks:
  annotations: true

coverage:
  range: "60...80"
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: 80%

comment:
  hide_project_coverage: false
